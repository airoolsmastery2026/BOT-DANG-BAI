# Backend scheduler example only (server/scheduler-example.js)
FACEBOOK_TOKEN=

# Security: never expose client secrets through REACT_APP_* variables.
# CRA embeds every REACT_APP_* value into the public browser bundle.
